get:
  summary: test Endpoint
  operationId: getTest
  tags:
    - Test
  responses:
    200:
      description: success
      content:
        application/json:
          schema:
            $ref: "../schemas/testResponseDTO.yaml"
